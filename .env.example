# environment variables

# GitHub API Configuration (Optional but recommended for higher rate limits)
# Without authentication: 60 requests per hour per IP
# With authentication: 5000 requests per hour
#
# How to create a GitHub Personal Access Token:
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Generate new token" -> "Generate new token (classic)"
# 3. Give it a descriptive name like "FOSS Vital API"
# 4. Select scopes: 
#    - public_repo (for public repositories)
#    - repo (if you need access to private repositories)
# 5. Copy the generated token and paste it below
#
# Token formats:
# - Personal Access Tokens start with 'ghp_'
# - GitHub App tokens start with 'ghs_'
# - Classic tokens are 40-character hex strings
GITHUB_TOKEN=your_github_personal_access_token_here

# GitHub API URL (usually doesn't need to be changed)
# GITHUB_API_URL=https://api.github.com

# Server Configuration (Local Development Only)
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Cache Configuration (optional)
# CACHE_TTL=259200000    # 3 days in milliseconds
# CACHE_MAX_SIZE=10000   # Maximum number of cached items

# For Vercel Deployment:
# Set GITHUB_TOKEN in Vercel Dashboard -> Settings -> Environment Variables
# NODE_ENV=production
# CORS_ORIGINS=https://your-vercel-app.vercel.app,https://your-domain.com
